//[2] Assignment Operators: These operators assign values to variables.

// Assignment (=)
// Addition assignment (+=)
// Subtraction assignment (-=)
// Multiplication assignment (*=)
// Division assignment (/=)
// Modulus assignment (%=)



// Addition assignment (+=)
let add = 0;
add += 1 // [if I break it then it shows like, add = add + 1]
add += 1
add += 1
add += 1
add += 1
console.log('The result is :',add);

// Subtraction assignment (-=)
let sub = 10;
sub -= 1; // [if I break it then it shows like, sub = sub + 1]
sub -= 1;
sub -= 1;
console.log('The result is :',sub);

// rest is same as like this.